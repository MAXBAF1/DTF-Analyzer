CREATE TABLE posts (
    id INTEGER PRIMARY KEY,
    title TEXT,
    published_at INTEGER,
    author TEXT
);

CREATE TABLE metrics (
    post_id INTEGER,
    ts INTEGER,              -- время замера
    minutes_since_publish REAL,
    views INTEGER,
    likes INTEGER,
    comments INTEGER,
    PRIMARY KEY (post_id, ts)
);